const listPostulation = require('./listPostulation.controller');
const newPostulation = require('./newPostulation.controller');
const postulationDetails = require('./postulationDetails.controller');

module.exports = { listPostulation, newPostulation, postulationDetails };
